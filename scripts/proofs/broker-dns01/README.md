# Historical DNS-01 proof artifacts

This directory captures a superseded broker experiment. Its scripts,
manifests, solver, and evidence are historical only and must not be executed
or treated as the production contract.

The supported contract is documented in
`docs/proofs/broker-dns01.md` and exercised by the production Go solver tests
plus the `managed-install` end-to-end shard.
