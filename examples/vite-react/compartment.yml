name: vite-react

services:
  web:
    path: .
    build:
      strategy: railpack
      command: node build.mjs
      env:
        - VITE_PUBLIC_GREETING
    readiness:
      type: http
      path: /ready
      timeoutMs: 30000
