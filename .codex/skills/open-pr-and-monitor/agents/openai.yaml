interface:
  display_name: 'Open PR and Monitor'
  short_description: 'Open or reuse a PR, then monitor it with a Codex heartbeat'
  default_prompt: 'Use $open-pr-and-monitor to open or reuse a PR, return its URL, and create a five-minute current-thread heartbeat that checks it until it is ready, terminal, or blocked.'
