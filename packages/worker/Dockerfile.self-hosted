# syntax=docker/dockerfile:1.7

ARG COMPARTMENT_NODE_BUILD_IMAGE=node:24.15.0-bookworm
ARG COMPARTMENT_NODE_RUNTIME_IMAGE=node:24.15.0-bookworm-slim
ARG COMPARTMENT_BUILDKIT_VERSION=v0.30.0
ARG COMPARTMENT_GO_BUILD_IMAGE=golang:1.26.5-bookworm

FROM ${COMPARTMENT_NODE_BUILD_IMAGE} AS build

RUN corepack enable
WORKDIR /workspace

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml tsconfig.base.json tsconfig.build.base.json ./
COPY .husky/install.mjs ./.husky/install.mjs
COPY packages ./packages

RUN HUSKY=0 pnpm install --frozen-lockfile
RUN pnpm --filter=@compartment/worker... build
RUN HUSKY=0 NODE_ENV=production pnpm --filter=@compartment/worker deploy --legacy --prod /runtime

FROM --platform=$BUILDPLATFORM ${COMPARTMENT_GO_BUILD_IMAGE} AS buildctl-build

ARG COMPARTMENT_BUILDKIT_VERSION
ARG TARGETARCH
ARG TARGETOS

ENV GOTOOLCHAIN=local

WORKDIR /workspace

COPY --chmod=755 packages/worker/scripts/build-buildctl.sh /usr/local/bin/build-buildctl

RUN apt-get update \
  && apt-get install --yes --no-install-recommends ca-certificates git \
  && rm -rf /var/lib/apt/lists/*

RUN build-buildctl

FROM --platform=$BUILDPLATFORM ${COMPARTMENT_GO_BUILD_IMAGE} AS railpack-build

ARG COMPARTMENT_RAILPACK_VERSION=v0.23.0
ARG TARGETARCH
ARG TARGETOS

ENV GOTOOLCHAIN=local

WORKDIR /workspace

COPY --chmod=755 packages/worker/scripts/build-railpack.sh /usr/local/bin/build-railpack

RUN apt-get update \
  && apt-get install --yes --no-install-recommends ca-certificates git \
  && rm -rf /var/lib/apt/lists/*

RUN build-railpack

FROM --platform=$BUILDPLATFORM ${COMPARTMENT_GO_BUILD_IMAGE} AS runc-launcher-build

ARG TARGETARCH
ARG TARGETOS

ENV CGO_ENABLED=0 GOARCH=${TARGETARCH} GOOS=${TARGETOS} GOTOOLCHAIN=local

WORKDIR /workspace

COPY packages/worker/scripts/runc-no-new-keyring.go ./runc-no-new-keyring.go

RUN go build -trimpath -ldflags='-s -w' -o /out/runc-no-new-keyring ./runc-no-new-keyring.go

FROM --platform=$TARGETPLATFORM debian:bookworm-slim AS syft-install

ARG COMPARTMENT_SYFT_VERSION=1.44.0
ARG TARGETARCH

RUN apt-get update \
  && apt-get install --yes --no-install-recommends ca-certificates curl \
  && case "${TARGETARCH}" in \
    amd64) SYFT_SHA256='0e91737aee2b5baf1d255b959630194a302335d848ff97bb07921eb6205b5f5a' ;; \
    arm64) SYFT_SHA256='6f6cdcdc695721d91ce756e3b5bc3e3416599c464101f5e32e9c3f33054ee6d9' ;; \
    *) exit 1 ;; \
  esac \
  && curl --fail --location --silent --show-error \
    --output /tmp/syft.tar.gz \
    "https://github.com/anchore/syft/releases/download/v${COMPARTMENT_SYFT_VERSION}/syft_${COMPARTMENT_SYFT_VERSION}_linux_${TARGETARCH}.tar.gz" \
  && printf '%s  %s\n' "${SYFT_SHA256}" /tmp/syft.tar.gz | sha256sum --check --strict \
  && tar --extract --gzip --file /tmp/syft.tar.gz --directory /usr/local/bin syft

FROM ${COMPARTMENT_NODE_RUNTIME_IMAGE} AS runtime

ARG COMPARTMENT_RUNTIME_UID=10001
ARG COMPARTMENT_RUNTIME_GID=10001

ENV NODE_ENV=production

LABEL org.opencontainers.image.source="https://github.com/compartmentdev/compartment"

COPY --from=buildctl-build /out/buildctl /usr/local/bin/buildctl
COPY --from=railpack-build /out/railpack /usr/local/bin/railpack
COPY --from=runc-launcher-build /out/runc-no-new-keyring /usr/local/bin/runc-no-new-keyring
COPY --from=syft-install /usr/local/bin/syft /usr/local/bin/syft

RUN apt-get update \
  && apt-get upgrade --yes --no-install-recommends \
  && apt-get install --yes --no-install-recommends ca-certificates dumb-init tar \
  && rm -rf /usr/local/lib/node_modules/npm /usr/local/bin/npm /usr/local/bin/npx \
  && rm -rf /var/lib/apt/lists/*

RUN groupadd --gid "${COMPARTMENT_RUNTIME_GID}" compartment-runtime \
  && useradd --uid "${COMPARTMENT_RUNTIME_UID}" --gid "${COMPARTMENT_RUNTIME_GID}" --home-dir /app --no-create-home --shell /usr/sbin/nologin compartment-runtime

WORKDIR /app

COPY --chown=compartment-runtime:compartment-runtime --from=build /runtime /app

USER compartment-runtime

ENTRYPOINT ["dumb-init", "--"]
CMD ["node", "dist/server.js"]
